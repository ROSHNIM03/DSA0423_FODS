import pandas as pd
from sklearn.cluster import KMeans

data = pd.DataFrame({
'spending':[100,200,150,400,500],
'visits':[5,8,6,10,12]
})

kmeans = KMeans(n_clusters=2)
data['Cluster'] = kmeans.fit_predict(data)

print(data)
